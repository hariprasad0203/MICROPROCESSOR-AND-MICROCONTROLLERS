   OUTPUT 2500AD
.ORG 2000H
DATA SEGMENT
A DB 08H
B DB 10H
C DB 02H
RESULT DB 00H
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE,DS:DATA
	MOV AX,DATA
	MOV DS,AX
	MOV AL,B
	MUL AL
	MOV DX,AX
	MOV AL,A
	MUL C
	MOV CX,AX
	MOV AX,CX
	MOV BX,0004H
	MUL BX
	SUB BX,AX
	MOV AL,02H
	MUL A
	MOV CX,AX
	MOV AX,BX
	DIV CX
	MOV RESULT,AX	
	HLT
CODE ENDS
END
